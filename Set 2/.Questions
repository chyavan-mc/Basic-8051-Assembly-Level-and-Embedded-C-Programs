1. Write an ALP to add 'N' bytes of data taking into account the possible carry output

2. Write an ALP to add 'N' bytes of BCD numbers taking into account the possible carry out

3. Write an ALP to subtract two BCD numbers

4. Write an ALP to add two multibyte numbers

5. Write an ALP to find the average of 'N' bytes of data
